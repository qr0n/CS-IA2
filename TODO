Pseudocode Fixes:
    + Add `login`                                               # DONE
    + Add globally declared structs `MenuItem` `SalesRecord`    # DONE

Use of files / Justification
    + Show file formatting (username) (password)                # DONE
    + Data type of each input                                   # DONE
    + Who its coming from                                       # Pending
    + Purpose of each input
    
    + State outputs 
    + Purpose of each output

Testing:
    4.3:
    fix input and screenshot
    test with long int

    4.1:
    change test.type to `extreme`

Technical documentation:
    + Redo